Debug `{error}` in `{project}`.

Similar past sessions and context: {auto-context}.

Start by reproducing the failure, form a hypothesis, then fix the root cause —
not the symptom. Add a regression test that fails before the fix and passes after.
