Implement `{feature}` in `{project}`.

My typical approach and context: {auto-context}.

Plan the change first (files to touch, the public surface, edge cases), confirm
the plan, then implement with tests. Keep it consistent with the surrounding code.
